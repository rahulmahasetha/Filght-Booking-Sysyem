{
  "version": 2,
  "buildCommand": "pip install --no-cache-dir -r requirements.txt && python myproject/manage.py collectstatic --noinput --clear",
  "builds": [
    {
      "src": "myproject/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "runtime": "python3.12",
        "missingFrameworkWarning": false
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "myproject/wsgi.py"
    }
  ],
  "env": {
    "DJANGO_SETTINGS_MODULE": "myproject.settings",
    "PYTHONUNBUFFERED": "1"
  }
}
